default:
  tags:
    - shellrunner


stages:
  - build
  - test

build-job:
  stage: build
  script:
    - python --version
    - pip --version
    - pip install -r requirements.txt
    - pytest --version

pytest-job:
  stage: test
  script:
    - pytest -v -s